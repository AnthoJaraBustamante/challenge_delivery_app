 
import 'package:get/instance_manager.dart';

class DependencyInjection {
  static void init() {
    // Get.put(Dio(BaseOptions(baseUrl: ApiBaseURL.baseUrl)));

    ///PROVIDERS

    // Get.put(LoginApi());
    // Get.put(RegisterApi());

    ///SERVICES

    // Get.put(LoginService());
    // Get.put(RegisterService());
  }
}
